# Database
DATABASE_URL="postgresql://groww:groww@localhost:5432/groww?schema=public"

# Auth — use a long random string in production
AUTH_SECRET="replace-with-a-long-random-secret-at-least-32-chars"

# Finnhub — free tier: https://finnhub.io/register
FINNHUB_API_KEY="your-finnhub-api-key"

# Optional: enable deterministic dev fallback when FINNHUB_API_KEY is unset
# USE_DEV_MARKET_DATA_FALLBACK="true"
